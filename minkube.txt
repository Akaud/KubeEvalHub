kubectl delete namespace load-lab
minikube stop
minikube delete

kubectl create namespace kubeevalhub-agent
kubectl create secret generic kubeevalhub-agent-secret \
  -n kubeevalhub-agent \
  --from-literal=AGENT_TOKEN='b0a522a423366fe1b8c6a827c9c638204029f738e08dbf8c9dcb9c79ad28e461'

# save the deployment manifest to agent.yaml, then apply it
kubectl apply -f agent.yaml