apiVersion: v1
kind: Namespace
metadata:
  name: kubeevalhub-agent

---
apiVersion: v1
kind: Secret
metadata:
  name: kubeevalhub-agent-secret
  namespace: kubeevalhub-agent
type: Opaque
stringData:
  AGENT_TOKEN: "3b3bfce1d9ef92198c0081996eb00251fb1385d15b1eef29b0b6d65dca74bf79"

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kubeevalhub-agent
  namespace: kubeevalhub-agent

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kubeevalhub-agent
rules:
  - apiGroups: [""]
    resources: ["nodes", "pods"]
    verbs: ["get", "list"]
  - apiGroups: ["metrics.k8s.io"]
    resources: ["nodes", "pods"]
    verbs: ["get", "list"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kubeevalhub-agent
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kubeevalhub-agent
subjects:
  - kind: ServiceAccount
    name: kubeevalhub-agent
    namespace: kubeevalhub-agent

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubeevalhub-agent
  namespace: kubeevalhub-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubeevalhub-agent
  template:
    metadata:
      labels:
        app: kubeevalhub-agent
    spec:
      serviceAccountName: kubeevalhub-agent
      containers:
        - name: agent
          image: kubeevalhub-agent:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: BACKEND_URL
              value: "http://host.minikube.internal:5000"
            - name: AGENT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: kubeevalhub-agent-secret
                  key: AGENT_TOKEN
            - name: SCRAPE_INTERVAL
              value: "30s"
            - name: REQUEST_TIMEOUT
              value: "10s"
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"